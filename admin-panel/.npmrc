# =================================================================
# NPM Configuration for Dependency Confusion Attack Prevention
# =================================================================

# 기본 레지스트리: 공식 NPM만 사용
registry=https://registry.npmjs.org/

# Prisma 스코프 패키지는 공식 레지스트리에서만 설치
@prisma:registry=https://registry.npmjs.org/

# [추가] .prisma 로컬 패키지가 외부 레지스트리에서 조회되지 않도록 차단
# 존재하지 않는 로컬 주소로 레지스트리를 지정하여 외부 유출 및 혼동 공격을 방지합니다.
.prisma:registry=http://localhost:4873

# AWS Amplify 스코프 패키지는 공식 레지스트리에서만 설치
@aws-amplify:registry=https://registry.npmjs.org/

# AWS SDK 스코프 패키지는 공식 레지스트리에서만 설치
@aws-sdk:registry=https://registry.npmjs.org/

# Nuxt 스코프 패키지는 공식 레지스트리에서만 설치
@nuxtjs:registry=https://registry.npmjs.org/

# Nuxt 코어 패키지는 공식 레지스트리에서만 설치
@nuxt:registry=https://registry.npmjs.org/

# VueUse 패키지는 공식 레지스트리에서만 설치
@vueuse:registry=https://registry.npmjs.org/

# TypeScript ESLint 패키지는 공식 레지스트리에서만 설치
@typescript-eslint:registry=https://registry.npmjs.org/

# =================================================================
# 보안 설정
# =================================================================

# package-lock.json 사용 강제 (정확한 버전 고정)
package-lock=true

# 엄격한 SSL 체크 (Man-in-the-Middle 공격 방어)
strict-ssl=true

# 로컬 캐시 검증
cache-verify=true

# =================================================================
# 추가 보안 옵션
# =================================================================

# 설치 전 스크립트 자동 실행 방지 (악성 코드 실행 방지)
ignore-scripts=false

# 정확한 버전만 설치 (^, ~ 같은 범위 무시)
save-exact=false

# 감사(audit) 자동 실행
audit=true